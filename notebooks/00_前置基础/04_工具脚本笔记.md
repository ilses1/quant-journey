# 放弃 conda 和 Miniconda？

安装之后 三方源 都不可用 官方源 开了vpn之后 也经常下载不成功 放弃这个

- 使用本地python 和 pip

# %% 是 VS Code 中 Python 交互式编程的“魔法指令”。

它被称为 “单元格标记”（Cell Marker）。只要在 Python 脚本（.py 文件）中加入 # %%，VS Code 就会自动把它识别为一个 Jupyter Notebook 风格的代码块。

# 调试断点

量化的 bug 往往不是"语法错误"而是"逻辑错误"——信号算错了、时间对齐偏了、数据选错了。这时候 `print` 调试效率太低。

1. 在行号左侧点击，添加断点（红点）
2. 按 `F5` 启动调试
3. 程序运行到断点处暂停，左侧面板可查看所有变量值
4. `F10` 逐行执行，`F11` 进入函数内部

| 库           | 数据范围                     |      是否需要注册      |      推荐度       |
| ------------ | ---------------------------- | :--------------------: | :---------------: |
| **AKShare**  | A股/期货/期权/基金/宏观/外汇 |           否           | ⭐⭐⭐ 最全免费源 |
| **Tushare**  | A股日线/分钟线/财务数据      | 是（免费注册，积分制） |  ⭐⭐⭐ 稳定老牌  |
| **Baostock** | A股历史日线/分钟线/财务      |           否           |   ⭐⭐ 完全免费   |
| **yfinance** | 美股/港股/全球指数           |           否           |   ⭐⭐ 国际数据   |

TA-Lib 在 Windows 上安装可能报错（缺少 C 编译环境）。解决方案：

**方案1（推荐）：下载预编译包**
访问 [https://www.lfd.uci.edu/~gohlke/pythonlibs/#ta-lib](https://www.lfd.uci.edu/~gohlke/pythonlibs/#ta-lib)，
下载对应 Python 版本的 `.whl` 文件，然后本地安装：

https://github.com/cgohlke/talib-build/releases#release-v0.6.7

把 whl 文件放到纯英文路径下，CMD 进入该目录执行：

```powershell
pip install TA_Lib‑0.4.28‑cp310‑cp310‑win_amd64.whl
```

> `cp310` = Python 3.10，`win_amd64` = Windows 64位。根据自己的 Python 版本选择。
